FROM nginx:alpine

RUN rm -rf /usr/share/nginx/html/*
COPY ./Reactjs/ /usr/share/nginx/html/

# entrypoint script (must be executable)
COPY ./99-env.sh ./frontend/99-env.sh
RUN chmod +x ./frontend/99-env.sh
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]